# 使用模块中的函数
# 模块是python程序架构的一个核心概念
# 模块就好比是工具包，要想使用这个工具包中的工具，就需要导入import这个模块
# 每一个以扩展名 py 结尾的 python源代码文件都是一个模块
# 在模块中定义的全局变量，函数 都是模块能够提供=给外界直接使用的工具


# 大白话讲：
# 每一个 .py结尾的文件都是一个模块，都可以使用import使用这个模块
# 而这个模块中的函数，全局变量都是可以供调用者使用的工具