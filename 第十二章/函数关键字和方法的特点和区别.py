# 关键字，函数和方法

# 关键字：
# 关键字是python内置的，具有特殊意义的标识符
# 打开python交互模式:
import keyword
print(keyword.kwlist)
print(len(keyword.kwlist))
# 可以看到很多关键字
# 关键字后面不需要哦使用括号


# 函数
# 函数 封装了独立功能，可以直接调用
# 调用函数：
# 函数名(参数)



# 方法
# 方法和函数类似，同样是封装了独立的功能
# 方法 需要通过对象来调用，表示针对这个对象要做的操作

# 对象,方法名(参数)
# 在变量后面输入.，然后选择针对这个变量要执行的操作即可
